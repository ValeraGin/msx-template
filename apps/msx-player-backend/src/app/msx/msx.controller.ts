import { Controller, Get } from '@nestjs/common';
import { environment } from '../../environments/environment';

@Controller('msx')
export class MsxController {
  @Get('start.json')
  getStart() {
    return {
      name: `MSX Player Template (${environment.production ? 'PROD' : 'DEV'})`,
      version: '1.0.0',
      parameter: `menu:request:interaction:init@${environment.pluginsUrl}/index.html`,
      note: "this file generated by NestJS",
      welcome: 'none'
    };
  }
}
